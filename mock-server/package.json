{
  "name": "ecom-mock-server",
  "version": "1.0.0",
  "description": "Mock API Server for E-commerce using JSON Server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "dev": "json-server --watch db.json --port 3000 --delay 500"
  },
  "keywords": ["mock", "api", "json-server"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
